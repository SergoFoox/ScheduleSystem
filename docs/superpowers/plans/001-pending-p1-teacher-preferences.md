# План впровадження: Преференції викладачів (UI та Зберігання)

> **Для агентів:** ОБОВ'ЯЗКОВА ПІД-НАВИЧКА: Використовуйте `superpowers:subagent-driven-development` (рекомендовано) або `superpowers:executing-plans` для виконання цього плану крок за кроком. Кроки використовують синтаксис чекбоксів (`- [ ]`) для відстеження прогресу.

**Мета:** Реалізувати можливість налаштування детальної матриці доступності викладачів (бажані/неможливі часові слоти) без зміни алгоритму розкладу.

**Архітектура:** Створення сутності `TeacherAvailability` для зберігання статусів по днях тижня та номерах пар, створення інтерактивного UI-діалогу на React для редагування матриці.

**Технологічний стек:** Java 21, Spring Boot, Vaadin 25, React, PostgreSQL.

---

### Таблиця файлів

| Файл | Відповідальність | Дія |
| :--- | :--- | :--- |
| `src/main/resources/db/migration/V10__Teacher_Availability.sql` | Схема бази даних | Створити |
| `src/main/java/com/sergofoox/domain/teacher/AvailabilityStatus.java` | Enum статусів | Створити |
| `src/main/java/com/sergofoox/domain/teacher/TeacherAvailability.java` | Entity доступності | Створити |
| `src/main/java/com/sergofoox/domain/teacher/TeacherAvailabilityRepository.java` | Репозиторій | Створити |
| `src/main/java/com/sergofoox/domain/ui/dto/AvailabilityDTO.java` | DTO для обміну | Створити |
| `src/main/java/com/sergofoox/domain/teacher/TeacherAvailabilityEndpoint.java` | API для фронтенду | Створити |
| `src/main/java/com/sergofoox/domain/teacher/Teacher.java` | Доменна модель викладача | Оновити |
| `src/main/frontend/components/AvailabilityDialog.tsx` | UI матриці доступності | Створити |
| `src/main/frontend/views/teachers.tsx` | Список викладачів | Оновити |

---

### Завдання 1: База даних та Доменна модель

**Files:**
- Create: `src/main/resources/db/migration/V10__Teacher_Availability.sql`
- Create: `src/main/java/com/sergofoox/domain/teacher/AvailabilityStatus.java`
- Create: `src/main/java/com/sergofoox/domain/teacher/TeacherAvailability.java`
- Modify: `src/main/java/com/sergofoox/domain/teacher/Teacher.java`

- [ ] **Крок 1: Створити SQL міграцію V10**
```sql
CREATE TABLE teacher_availability (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    teacher_id BIGINT NOT NULL,
    day_of_week VARCHAR(20) NOT NULL,
    lesson_number INTEGER NOT NULL,
    status VARCHAR(20) NOT NULL,
    CONSTRAINT fk_availability_teacher FOREIGN KEY (teacher_id) REFERENCES teacher(id) ON DELETE CASCADE
);
CREATE INDEX idx_teacher_avail_teacher ON teacher_availability(teacher_id);
```

- [ ] **Крок 2: Створити AvailabilityStatus Enum**
```java
package com.sergofoox.domain.teacher;

public enum AvailabilityStatus {
    PREFERRED, UNAVAILABLE
}
```

- [ ] **Крок 3: Створити TeacherAvailability Entity**
```java
package com.sergofoox.domain.teacher;

import jakarta.persistence.*;
import jakarta.validation.constraints.NotNull;
import java.time.DayOfWeek;

@Entity
public class TeacherAvailability {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @NotNull @ManyToOne(fetch = FetchType.LAZY)
    private Teacher teacher;

    @NotNull @Enumerated(EnumType.STRING)
    private DayOfWeek dayOfWeek;

    @NotNull private Integer lessonNumber;

    @NotNull @Enumerated(EnumType.STRING)
    private AvailabilityStatus status;
    
    public TeacherAvailability() {}
    
    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public Teacher getTeacher() { return teacher; }
    public void setTeacher(Teacher teacher) { this.teacher = teacher; }
    public DayOfWeek getDayOfWeek() { return dayOfWeek; }
    public void setDayOfWeek(DayOfWeek dayOfWeek) { this.dayOfWeek = dayOfWeek; }
    public Integer getLessonNumber() { return lessonNumber; }
    public void setLessonNumber(Integer lessonNumber) { this.lessonNumber = lessonNumber; }
    public AvailabilityStatus getStatus() { return status; }
    public void setStatus(AvailabilityStatus status) { this.status = status; }
}
```

- [ ] **Крок 4: Додати зв'язок у Teacher.java**
```java
    @OneToMany(mappedBy = "teacher", cascade = CascadeType.ALL, orphanRemoval = true)
    private List<TeacherAvailability> availability = new java.util.ArrayList<>();
    
    public List<TeacherAvailability> getAvailability() { return availability; }
    public void setAvailability(List<TeacherAvailability> availability) { this.availability = availability; }
```

---

### Завдання 2: Репозиторій, DTO та Ендпоінт

**Files:**
- Create: `src/main/java/com/sergofoox/domain/teacher/TeacherAvailabilityRepository.java`
- Create: `src/main/java/com/sergofoox/domain/ui/dto/AvailabilityDTO.java`
- Create: `src/main/java/com/sergofoox/domain/teacher/TeacherAvailabilityEndpoint.java`

- [ ] **Крок 1: Створити Репозиторій**
```java
package com.sergofoox.domain.teacher;

import org.springframework.data.jpa.repository.JpaRepository;
import java.util.List;

public interface TeacherAvailabilityRepository extends JpaRepository<TeacherAvailability, Long> {
    List<TeacherAvailability> findByTeacherId(Long teacherId);
    void deleteByTeacherId(Long teacherId);
}
```

- [ ] **Крок 2: Створити DTO**
```java
package com.sergofoox.domain.ui.dto;

import com.sergofoox.domain.teacher.AvailabilityStatus;
import java.time.DayOfWeek;

public record AvailabilityDTO(DayOfWeek dayOfWeek, Integer lessonNumber, AvailabilityStatus status) {
}
```

- [ ] **Крок 3: Створити Ендпоінт**
```java
package com.sergofoox.domain.teacher;

import com.sergofoox.domain.ui.TemplateAccessService;
import com.sergofoox.domain.ui.dto.AvailabilityDTO;
import com.vaadin.flow.server.auth.AnonymousAllowed;
import com.vaadin.hilla.BrowserCallable;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

@BrowserCallable
@AnonymousAllowed
public class TeacherAvailabilityEndpoint {
    private final TeacherAvailabilityRepository repository;
    private final TeacherRepository teacherRepository;
    private final TemplateAccessService templateAccessService;

    public TeacherAvailabilityEndpoint(TeacherAvailabilityRepository repository,
                                       TeacherRepository teacherRepository,
                                       TemplateAccessService templateAccessService) {
        this.repository = repository;
        this.teacherRepository = teacherRepository;
        this.templateAccessService = templateAccessService;
    }

    public List<AvailabilityDTO> getAvailability(Long teacherId) {
        return repository.findByTeacherId(teacherId).stream()
                .map(a -> new AvailabilityDTO(a.getDayOfWeek(), a.getLessonNumber(), a.getStatus()))
                .toList();
    }

    @Transactional
    public void saveAvailability(Long teacherId, List<AvailabilityDTO> dtos) {
        templateAccessService.requireWritableTemplate();
        repository.deleteByTeacherId(teacherId);
        Teacher teacher = teacherRepository.findById(teacherId).orElseThrow();
        List<TeacherAvailability> entities = dtos.stream().map(dto -> {
            TeacherAvailability a = new TeacherAvailability();
            a.setTeacher(teacher);
            a.setDayOfWeek(dto.dayOfWeek());
            a.setLessonNumber(dto.lessonNumber());
            a.setStatus(dto.status());
            return a;
        }).toList();
        repository.saveAll(entities);
    }
}
```

---

### Завдання 3: UI Компоненти

**Files:**
- Create: `src/main/frontend/components/AvailabilityDialog.tsx`
- Modify: `src/main/frontend/views/teachers.tsx`

- [ ] **Крок 1: Створити AvailabilityDialog.tsx**
```tsx
import React, { useEffect, useState } from 'react';
import { Dialog } from '@vaadin/react-components/Dialog.js';
import { Button } from '@vaadin/react-components/Button.js';
import { Icon } from '@vaadin/react-components/Icon.js';
import { HorizontalLayout } from '@vaadin/react-components/HorizontalLayout.js';
import { Notification } from '@vaadin/react-components/Notification.js';
import { TeacherAvailabilityEndpoint } from '../generated/endpoints';
import type AvailabilityDTO from '../generated/com/sergofoox/domain/ui/dto/AvailabilityDTO';
import AvailabilityStatus from '../generated/com/sergofoox/domain/teacher/AvailabilityStatus';
import type TeacherDTO from '../generated/com/sergofoox/domain/ui/dto/TeacherDTO';

interface AvailabilityDialogProps {
  opened: boolean;
  teacher?: TeacherDTO;
  onClose: () => void;
}

const DAYS = ['MONDAY', 'TUESDAY', 'WEDNESDAY', 'THURSDAY', 'FRIDAY', 'SATURDAY'];
const LESSON_NUMBERS = [1, 2, 3, 4, 5, 6];

export const AvailabilityDialog: React.FC<AvailabilityDialogProps> = ({ opened, teacher, onClose }) => {
  const [availabilities, setAvailabilities] = useState<AvailabilityDTO[]>([]);
  const [saving, setSaving] = useState(false);

  useEffect(() => {
    if (opened && teacher?.id) {
      TeacherAvailabilityEndpoint.getAvailability(teacher.id as any)
        .then(data => setAvailabilities(data || []))
        .catch(err => console.error('Failed to load availability:', err));
    }
  }, [opened, teacher]);

  const getStatus = (day: string, lesson: number) => {
    return availabilities.find(a => a.dayOfWeek === day && a.lessonNumber === lesson)?.status;
  };

  const toggleStatus = (day: string, lesson: number) => {
    const current = getStatus(day, lesson);
    let next: AvailabilityStatus | undefined;
    if (!current) next = AvailabilityStatus.PREFERRED;
    else if (current === AvailabilityStatus.PREFERRED) next = AvailabilityStatus.UNAVAILABLE;
    else next = undefined;

    const filtered = availabilities.filter(a => !(a.dayOfWeek === day && a.lessonNumber === lesson));
    if (next) {
      setAvailabilities([...filtered, { dayOfWeek: day as any, lessonNumber: lesson, status: next }]);
    } else {
      setAvailabilities(filtered);
    }
  };

  const handleSave = async () => {
    if (!teacher?.id) return;
    setSaving(true);
    try {
      await TeacherAvailabilityEndpoint.saveAvailability(teacher.id as any, availabilities);
      Notification.show('Преференції збережено', { theme: 'success' });
      onClose();
    } catch (err) {
      console.error('Failed to save availability:', err);
      Notification.show('Помилка збереження', { theme: 'error' });
    } finally {
      setSaving(false);
    }
  };

  const renderCell = (day: string, lesson: number) => {
    const status = getStatus(day, lesson);
    let color = 'bg-white';
    let icon = null;
    if (status === AvailabilityStatus.PREFERRED) {
      color = 'bg-green-100 text-green-700';
      icon = <Icon icon="vaadin:star" className="w-4 h-4" />;
    } else if (status === AvailabilityStatus.UNAVAILABLE) {
      color = 'bg-red-100 text-red-700';
      icon = <Icon icon="vaadin:close" className="w-4 h-4" />;
    }

    return (
      <div
        key={`${day}-${lesson}`}
        onClick={() => toggleStatus(day, lesson)}
        className={`border p-4 flex items-center justify-center cursor-pointer transition-colors hover:bg-gray-50 ${color}`}
        title={`${day}, пара ${lesson}`}
      >
        {icon}
      </div>
    );
  };

  return (
    <Dialog
      headerTitle={`Преференції: ${teacher?.fullName}`}
      opened={opened}
      onOpenedChanged={(e) => !e.detail.value && onClose()}
      footerRenderer={() => (
        <HorizontalLayout theme="spacing">
          <Button theme="primary" onClick={handleSave} disabled={saving}>
            Зберегти
          </Button>
          <Button onClick={onClose}>Закрити</Button>
        </HorizontalLayout>
      )}
    >
      <div className="flex flex-col gap-4">
        <div className="flex gap-4 text-xs font-medium text-gray-500 mb-2">
            <div className="flex items-center gap-1"><div className="w-3 h-3 bg-green-100 border"></div> Бажано</div>
            <div className="flex items-center gap-1"><div className="w-3 h-3 bg-red-100 border"></div> Неможливо</div>
            <div className="flex items-center gap-1"><div className="w-3 h-3 bg-white border"></div> Нейтрально</div>
        </div>
        <div className="grid grid-cols-7 border">
          <div className="border bg-gray-50 p-2 font-bold text-center">#</div>
          {DAYS.map(day => (
            <div key={day} className="border bg-gray-50 p-2 font-bold text-center text-xs">
              {day.substring(0, 3)}
            </div>
          ))}
          {LESSON_NUMBERS.map(lesson => (
            <React.Fragment key={lesson}>
              <div className="border bg-gray-50 p-2 font-bold text-center">{lesson}</div>
              {DAYS.map(day => renderCell(day, lesson))}
            </React.Fragment>
          ))}
        </div>
      </div>
    </Dialog>
  );
};
```

- [ ] **Крок 2: Оновити TeachersView.tsx**
Додати кнопку для відкриття діалогу та необхідні стани.

---

### Завдання 4: Перевірка

- [ ] **Крок 1: Запустити додаток та перевірити UI**
- [ ] **Крок 2: Перевірити збереження в БД**
